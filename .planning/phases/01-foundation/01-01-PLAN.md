---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - vitest.config.ts
  - tests/setup.ts
  - src/types/vessel.ts
  - src/types/ais.ts
  - src/lib/ais/parser.test.ts
  - src/lib/ais/filter.test.ts
  - src/lib/auth.test.ts
  - src/lib/db/positions.test.ts
  - src/lib/map/geojson.test.ts
  - src/lib/map/filter.test.ts
  - src/lib/map/tracks.test.ts
  - .env.example
autonomous: true
requirements:
  - DATA-03

must_haves:
  truths:
    - "Project builds and tests run without errors"
    - "TypeScript types exist for vessel and AIS data structures"
    - "Test scaffolds exist for all testable requirements"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "next"
    - path: "vitest.config.ts"
      provides: "Test configuration"
      contains: "vitest"
    - path: "src/types/vessel.ts"
      provides: "Vessel type definitions"
      exports: ["Vessel", "VesselPosition"]
    - path: "src/types/ais.ts"
      provides: "AIS message type definitions"
      exports: ["AISMessage", "PositionReport", "ShipStaticData"]
  key_links:
    - from: "vitest.config.ts"
      to: "tests/setup.ts"
      via: "setupFiles config"
      pattern: "setupFiles.*setup"
---

<objective>
Initialize Next.js project with TypeScript, install all dependencies, create type definitions, and scaffold test files for Nyquist compliance.

Purpose: Wave 0 establishes the foundation that all other plans build on. Type definitions provide contracts for executors. Test scaffolds ensure verification commands exist before implementation.

Output: Buildable Next.js project with all dependencies, TypeScript types, and test scaffolds.
</objective>

<execution_context>
@C:/Users/I771473/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/I771473/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with all dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, .env.example</files>
  <action>
Create Next.js 16 project with App Router. Install all dependencies from research:

```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-git
```

Then install additional dependencies:
```bash
npm install mapbox-gl pg bcrypt jose ws zustand date-fns
npm install -D @types/pg @types/bcrypt vitest @testing-library/react happy-dom @vitejs/plugin-react
```

Create `.env.example` with required environment variables:
```
DATABASE_URL=postgresql://user:password@host:5432/tanker_tracker
AISSTREAM_API_KEY=your_aisstream_api_key
JWT_SECRET=your_jwt_secret_min_32_chars
PASSWORD_HASH=bcrypt_hash_of_shared_password
MAPBOX_ACCESS_TOKEN=your_mapbox_token
```

Update `next.config.ts` to handle mapbox-gl CSS import and any needed transpile settings.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Next.js project builds successfully with all dependencies installed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TypeScript type definitions</name>
  <files>src/types/vessel.ts, src/types/ais.ts</files>
  <behavior>
    - Vessel type has: imo (string, PK), mmsi (string), name (string), flag (string), shipType (number), destination (string | null), lastSeen (Date)
    - VesselPosition type has: time (Date), mmsi (string), imo (string | null), latitude (number), longitude (number), speed (number | null), course (number | null), heading (number | null), navStatus (number | null), lowConfidence (boolean)
    - AISMessage discriminated union for PositionReport and ShipStaticData message types
    - PositionReport matches AISStream.io format: MessageType, Message (with Latitude, Longitude, Sog, Cog, TrueHeading, NavigationalStatus), MetaData (MMSI, time_utc)
    - ShipStaticData matches AISStream.io format: includes ImoNumber, ShipName, ShipType, Destination
  </behavior>
  <action>
Create type definitions that match AISStream.io message format and database schema.

In `src/types/vessel.ts`:
- Export `Vessel` interface with IMO as primary key
- Export `VesselPosition` interface matching TimescaleDB hypertable columns
- Export `VesselWithPosition` for API responses combining current position with metadata

In `src/types/ais.ts`:
- Export `AISMessage` discriminated union with `MessageType` discriminator
- Export `PositionReport` interface matching AISStream.io PositionReport message
- Export `ShipStaticData` interface matching AISStream.io ShipStaticData message
- Export `AISMetaData` interface for the MetaData field common to all messages
- Include JSDoc comments explaining field meanings (SOG = speed over ground in knots, COG = course over ground in degrees)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Type definitions compile without errors and are exported from both files</done>
</task>

<task type="auto">
  <name>Task 3: Create Vitest config and test scaffolds</name>
  <files>vitest.config.ts, tests/setup.ts, src/lib/ais/parser.test.ts, src/lib/ais/filter.test.ts, src/lib/auth.test.ts, src/lib/db/positions.test.ts, src/lib/map/geojson.test.ts, src/lib/map/filter.test.ts, src/lib/map/tracks.test.ts</files>
  <action>
Create Vitest configuration for Next.js:

`vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'happy-dom',
    setupFiles: ['./tests/setup.ts'],
    include: ['src/**/*.test.ts', 'src/**/*.test.tsx'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

`tests/setup.ts`:
- Import vitest globals
- Mock environment variables for tests
- Set up any global test utilities

Create test scaffold files with `describe` blocks and `it.todo()` for each test case:

`src/lib/ais/parser.test.ts` (DATA-01, DATA-03):
- it.todo: parses PositionReport message correctly
- it.todo: parses ShipStaticData message and extracts IMO
- it.todo: handles malformed messages gracefully

`src/lib/ais/filter.test.ts` (DATA-04):
- it.todo: rejects positions with speed > 50 knots
- it.todo: rejects impossible speed jumps (>100 knots inferred)
- it.todo: flags positions in GPS jamming zones as low_confidence

`src/lib/auth.test.ts` (AUTH-01):
- it.todo: verifyPassword returns true for correct password
- it.todo: verifyPassword returns false for incorrect password
- it.todo: createSession returns valid JWT
- it.todo: verifySession validates JWT correctly

`src/lib/db/positions.test.ts` (DATA-02):
- it.todo: insertPosition writes to database
- it.todo: upsertVessel updates vessel metadata

`src/lib/map/geojson.test.ts` (MAP-01):
- it.todo: converts vessel array to FeatureCollection
- it.todo: includes all required properties in features

`src/lib/map/filter.test.ts` (MAP-03):
- it.todo: filterTankers returns only ship types 80-89
- it.todo: filterTankers with showAll returns all vessels

`src/lib/map/tracks.test.ts` (MAP-04):
- it.todo: builds LineString from position history
- it.todo: includes timestamp in line coordinates for gradient

Add `test` script to package.json: `"test": "vitest"`
  </action>
  <verify>
    <automated>npm run test -- --run</automated>
  </verify>
  <done>Vitest runs and reports all tests as todo (skipped), no failures</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npx tsc --noEmit` passes with no type errors
- `npm run test -- --run` runs and shows todo tests
- All required environment variables documented in .env.example
</verification>

<success_criteria>
- Next.js 16 project with App Router builds successfully
- All dependencies from research installed (mapbox-gl, pg, bcrypt, jose, ws, vitest)
- TypeScript types exist for Vessel, VesselPosition, AISMessage variants
- Test scaffolds exist for all testable requirements (DATA-01 through DATA-04, AUTH-01, MAP-01, MAP-03, MAP-04)
- Vitest configured and runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
