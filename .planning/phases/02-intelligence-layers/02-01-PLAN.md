---
phase: 02-intelligence-layers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/db/schema.sql
  - src/lib/sanctions/matcher.ts
  - src/lib/sanctions/matcher.test.ts
  - src/lib/prices/fetcher.ts
  - src/lib/prices/fetcher.test.ts
  - src/lib/news/fetcher.ts
  - src/lib/news/fetcher.test.ts
  - src/lib/geo/chokepoints.ts
  - src/lib/geo/chokepoints.test.ts
autonomous: true
requirements: [INTL-01, INTL-02, INTL-03, MAP-06, MAP-07]

must_haves:
  truths:
    - "Test scaffolds exist for all Phase 2 requirements"
    - "Database schema includes vessel_sanctions, oil_prices, and news_items tables"
    - "Dependencies recharts, node-cron, papaparse are installed"
  artifacts:
    - path: "src/lib/sanctions/matcher.test.ts"
      provides: "Sanctions matching test scaffolds"
      contains: "describe"
    - path: "src/lib/prices/fetcher.test.ts"
      provides: "Oil price fetcher test scaffolds"
      contains: "describe"
    - path: "src/lib/news/fetcher.test.ts"
      provides: "News fetcher test scaffolds"
      contains: "describe"
    - path: "src/lib/geo/chokepoints.test.ts"
      provides: "Chokepoint detection test scaffolds"
      contains: "describe"
    - path: "src/lib/db/schema.sql"
      provides: "Extended database schema"
      contains: "vessel_sanctions"
  key_links:
    - from: "package.json"
      to: "node_modules"
      via: "npm install"
      pattern: "recharts.*papaparse.*node-cron"
---

<objective>
Install Phase 2 dependencies, extend database schema with intelligence layer tables, and create test scaffolds for all requirements.

Purpose: Wave 0 foundation ensures all subsequent plans have dependencies available, schema ready, and test targets defined (Nyquist compliance).
Output: Buildable project with recharts/papaparse/node-cron, extended schema.sql, and test files with it.todo() stubs.
</objective>

<execution_context>
@C:/Users/I771473/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/I771473/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-intelligence-layers/02-RESEARCH.md

<interfaces>
<!-- Key types and exports from Phase 1 that this plan extends -->

From src/types/vessel.ts:
```typescript
export interface Vessel {
  imo: string;
  mmsi: string;
  name: string;
  flag: string;
  shipType: number;
  destination: string | null;
  lastSeen: Date;
}
```

From src/lib/db/index.ts:
```typescript
export const pool: Pool;
export async function query<T extends QueryResultRow>(sql: string, params?: unknown[]): Promise<T[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 2 dependencies</name>
  <files>package.json</files>
  <action>
Install recharts, node-cron, and papaparse for Phase 2:

```bash
npm install recharts node-cron papaparse
npm install -D @types/papaparse
```

Verify installation:
- recharts: Sparkline charts for oil prices (INTL-02)
- node-cron: Scheduled sanctions/price updates
- papaparse: CSV parsing for OpenSanctions data (INTL-01)

Do NOT install @types/node-cron (not needed, has built-in types).
  </action>
  <verify>
    <automated>npm ls recharts node-cron papaparse && npm run build</automated>
  </verify>
  <done>Dependencies installed and project builds successfully</done>
</task>

<task type="auto">
  <name>Task 2: Extend database schema with intelligence tables</name>
  <files>src/lib/db/schema.sql</files>
  <action>
Add three new tables to the existing schema.sql (append after existing tables):

1. **vessel_sanctions** (INTL-01):
```sql
CREATE TABLE IF NOT EXISTS vessel_sanctions (
  imo VARCHAR(10) PRIMARY KEY,
  sanctioning_authority VARCHAR(10) NOT NULL,
  list_date DATE,
  reason TEXT,
  confidence VARCHAR(10) DEFAULT 'HIGH',
  source_url TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

2. **oil_prices** (INTL-02):
```sql
CREATE TABLE IF NOT EXISTS oil_prices (
  id SERIAL PRIMARY KEY,
  symbol VARCHAR(10) NOT NULL,
  price DECIMAL(10, 2) NOT NULL,
  change DECIMAL(10, 2),
  change_percent DECIMAL(5, 2),
  fetched_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_oil_prices_symbol_time ON oil_prices(symbol, fetched_at DESC);
```

3. **news_items** (INTL-03):
```sql
CREATE TABLE IF NOT EXISTS news_items (
  id SERIAL PRIMARY KEY,
  title TEXT NOT NULL,
  source VARCHAR(100),
  url TEXT NOT NULL UNIQUE,
  published_at TIMESTAMPTZ,
  relevance_score INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_news_items_time ON news_items(published_at DESC);
```

Add comment header for Phase 2 section. Use IF NOT EXISTS for idempotent schema application.
  </action>
  <verify>
    <automated>grep -q "vessel_sanctions" src/lib/db/schema.sql && grep -q "oil_prices" src/lib/db/schema.sql && grep -q "news_items" src/lib/db/schema.sql && echo "Schema verified"</automated>
  </verify>
  <done>Schema.sql extended with vessel_sanctions, oil_prices, and news_items tables</done>
</task>

<task type="auto">
  <name>Task 3: Create test scaffolds for all Phase 2 requirements</name>
  <files>
    src/lib/sanctions/matcher.ts,
    src/lib/sanctions/matcher.test.ts,
    src/lib/prices/fetcher.ts,
    src/lib/prices/fetcher.test.ts,
    src/lib/news/fetcher.ts,
    src/lib/news/fetcher.test.ts,
    src/lib/geo/chokepoints.ts,
    src/lib/geo/chokepoints.test.ts
  </files>
  <action>
Create stub module files and test scaffolds with it.todo() for each requirement:

**1. src/lib/sanctions/matcher.ts** - Empty export placeholder:
```typescript
export function normalizeIMO(imo: string): string {
  // TODO: Implement IMO normalization
  return imo;
}

export function matchVesselSanctions(imo: string): Promise<SanctionMatch | null> {
  // TODO: Implement sanctions lookup
  return Promise.resolve(null);
}
```

**2. src/lib/sanctions/matcher.test.ts** (INTL-01):
```typescript
import { describe, it, expect } from 'vitest';

describe('Sanctions Matcher', () => {
  describe('normalizeIMO', () => {
    it.todo('removes IMO prefix from IMO number');
    it.todo('pads IMO to 7 digits with leading zeros');
    it.todo('handles null/undefined input');
  });

  describe('matchVesselSanctions', () => {
    it.todo('returns sanction data when IMO matches OFAC list');
    it.todo('returns sanction data when IMO matches EU list');
    it.todo('returns null when IMO not found in any list');
    it.todo('returns highest confidence match when multiple sources');
  });
});
```

**3. src/lib/prices/fetcher.ts** - Empty export placeholder:
```typescript
export interface OilPrice {
  symbol: 'WTI' | 'BRENT';
  price: number;
  change: number;
  changePercent: number;
  timestamp: Date;
}

export function fetchOilPrices(): Promise<OilPrice[]> {
  return Promise.resolve([]);
}
```

**4. src/lib/prices/fetcher.test.ts** (INTL-02):
```typescript
import { describe, it, expect } from 'vitest';

describe('Oil Price Fetcher', () => {
  describe('fetchOilPrices', () => {
    it.todo('fetches WTI price from Alpha Vantage');
    it.todo('fetches Brent price from Alpha Vantage');
    it.todo('falls back to FRED when Alpha Vantage fails');
    it.todo('returns empty array on complete API failure');
  });

  describe('caching', () => {
    it.todo('returns cached price within 15 minutes');
    it.todo('fetches fresh price after cache expiry');
  });
});
```

**5. src/lib/news/fetcher.ts** - Empty export placeholder:
```typescript
export interface NewsItem {
  title: string;
  source: string;
  url: string;
  publishedAt: Date;
  relevanceScore: number;
}

export function fetchNews(): Promise<NewsItem[]> {
  return Promise.resolve([]);
}
```

**6. src/lib/news/fetcher.test.ts** (INTL-03):
```typescript
import { describe, it, expect } from 'vitest';

describe('News Fetcher', () => {
  describe('fetchNews', () => {
    it.todo('fetches headlines from NewsAPI');
    it.todo('filters headlines by oil/tanker keywords');
    it.todo('filters headlines by Middle East region');
    it.todo('returns max 15 headlines');
  });

  describe('relevance scoring', () => {
    it.todo('assigns higher score to headlines with multiple keywords');
    it.todo('assigns score 0 to headlines with no matching keywords');
  });
});
```

**7. src/lib/geo/chokepoints.ts** - Bounding box definitions:
```typescript
export interface ChokepointBounds {
  minLat: number;
  maxLat: number;
  minLon: number;
  maxLon: number;
}

export interface Chokepoint {
  id: string;
  name: string;
  bounds: ChokepointBounds;
}

export const CHOKEPOINTS: Record<string, Chokepoint> = {
  hormuz: {
    id: 'hormuz',
    name: 'Strait of Hormuz',
    bounds: { minLat: 26.0, maxLat: 27.0, minLon: 55.5, maxLon: 57.0 },
  },
  babel_mandeb: {
    id: 'babel_mandeb',
    name: 'Bab el-Mandeb',
    bounds: { minLat: 12.4, maxLat: 13.0, minLon: 43.0, maxLon: 43.7 },
  },
  suez: {
    id: 'suez',
    name: 'Suez Canal',
    bounds: { minLat: 29.8, maxLat: 31.3, minLon: 32.2, maxLon: 32.6 },
  },
};

export function isInChokepoint(lat: number, lon: number, bounds: ChokepointBounds): boolean {
  return lat >= bounds.minLat && lat <= bounds.maxLat &&
         lon >= bounds.minLon && lon <= bounds.maxLon;
}
```

**8. src/lib/geo/chokepoints.test.ts** (MAP-07):
```typescript
import { describe, it, expect } from 'vitest';
import { isInChokepoint, CHOKEPOINTS } from './chokepoints';

describe('Chokepoints', () => {
  describe('isInChokepoint', () => {
    it('returns true for point inside Hormuz bounds', () => {
      expect(isInChokepoint(26.5, 56.0, CHOKEPOINTS.hormuz.bounds)).toBe(true);
    });

    it('returns false for point outside Hormuz bounds', () => {
      expect(isInChokepoint(25.0, 56.0, CHOKEPOINTS.hormuz.bounds)).toBe(false);
    });

    it.todo('returns true for point inside Bab el-Mandeb bounds');
    it.todo('returns true for point inside Suez bounds');
    it.todo('returns false for point at edge of bounds (exclusive)');
  });

  describe('CHOKEPOINTS constant', () => {
    it('contains three chokepoints', () => {
      expect(Object.keys(CHOKEPOINTS)).toHaveLength(3);
    });

    it.todo('has valid bounds for each chokepoint');
  });
});
```

Create parent directories as needed. Ensure all test files import from vitest.
  </action>
  <verify>
    <automated>npm run test -- --run 2>&1 | grep -E "(PASS|FAIL|todo)" | head -20</automated>
  </verify>
  <done>Test scaffolds created for sanctions, prices, news, and chokepoints with passing todo stubs</done>
</task>

</tasks>

<verification>
Phase 2 Wave 0 verification:
1. `npm ls recharts node-cron papaparse` - all dependencies present
2. `npm run build` - project compiles successfully
3. `npm run test -- --run` - all tests pass (including todo stubs)
4. `grep -E "vessel_sanctions|oil_prices|news_items" src/lib/db/schema.sql` - all tables defined
</verification>

<success_criteria>
- recharts, node-cron, papaparse installed in package.json
- schema.sql contains vessel_sanctions, oil_prices, news_items tables with indexes
- Test files exist for matcher, fetcher (prices/news), and chokepoints
- `npm run build` succeeds
- `npm run test -- --run` passes (todo stubs don't fail)
</success_criteria>

<output>
After completion, create `.planning/phases/02-intelligence-layers/02-01-SUMMARY.md`
</output>
