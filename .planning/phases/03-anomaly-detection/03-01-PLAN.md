---
phase: 03-anomaly-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.sql
  - src/types/anomaly.ts
  - src/lib/geo/haversine.ts
  - src/lib/geo/haversine.test.ts
  - src/lib/detection/coverage-zones.ts
  - src/lib/detection/coverage-zones.test.ts
  - src/lib/geo/anchorages.ts
  - src/lib/geo/anchorages.test.ts
  - src/lib/db/anomalies.ts
  - src/lib/db/anomalies.test.ts
  - src/lib/db/watchlist.ts
  - src/lib/db/watchlist.test.ts
  - src/lib/db/alerts.ts
  - src/lib/db/alerts.test.ts
  - package.json
autonomous: true
requirements: [ANOM-01, ANOM-02, HIST-02]

must_haves:
  truths:
    - "Anomaly types are defined with discriminated unions for type safety"
    - "Coverage zones for Middle East terrestrial AIS are defined"
    - "Anchorage exclusion zones prevent false loitering alerts"
    - "Haversine distance calculation is accurate for nautical scenarios"
    - "Database schema supports anomalies, watchlist, and alerts storage"
  artifacts:
    - path: "src/types/anomaly.ts"
      provides: "Type definitions for Anomaly, Alert, WatchlistEntry"
      exports: ["AnomalyType", "Confidence", "Anomaly", "GoingDarkDetails", "LoiteringDetails", "DeviationDetails", "SpeedDetails", "WatchlistEntry", "Alert"]
    - path: "src/lib/detection/coverage-zones.ts"
      provides: "AIS coverage zone definitions"
      exports: ["COVERAGE_ZONES", "isInCoverageZone", "getCoverageZone"]
    - path: "src/lib/geo/anchorages.ts"
      provides: "Known anchorage polygon definitions"
      exports: ["ANCHORAGES", "isInAnchorage", "getAnchorage"]
    - path: "src/lib/geo/haversine.ts"
      provides: "Distance and bearing calculations"
      exports: ["haversineDistance", "calculateBearing"]
    - path: "src/lib/db/schema.sql"
      provides: "Phase 3 database tables"
      contains: ["vessel_anomalies", "watchlist", "alerts"]
  key_links:
    - from: "src/lib/detection/coverage-zones.ts"
      to: "src/lib/geo/chokepoints.ts"
      via: "Same bounding box pattern"
      pattern: "ChokepointBounds interface"
    - from: "src/lib/geo/anchorages.ts"
      to: "src/lib/geo/chokepoints.ts"
      via: "Same bounding box pattern"
      pattern: "minLat/maxLat/minLon/maxLon"
---

<objective>
Install uuid dependency, extend database schema with Phase 3 tables (vessel_anomalies, watchlist, alerts), create type definitions for anomaly detection system, implement geo utilities (haversine, coverage zones, anchorages), and create test scaffolds for all CRUD operations.

Purpose: Establish the data foundation and geographic utilities required by all subsequent detection and UI tasks.
Output: Schema extended, types defined, geo functions implemented with tests, CRUD stubs ready for implementation.
</objective>

<execution_context>
@C:/Users/I771473/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/I771473/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-anomaly-detection/03-CONTEXT.md
@.planning/phases/03-anomaly-detection/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase. -->

From src/lib/geo/chokepoints.ts:
```typescript
export interface ChokepointBounds {
  minLat: number;
  maxLat: number;
  minLon: number;
  maxLon: number;
}

export interface Chokepoint {
  id: string;
  name: string;
  bounds: ChokepointBounds;
}

export function isInChokepoint(lat: number, lon: number, bounds: ChokepointBounds): boolean;
```

From src/stores/vessel.ts:
```typescript
export interface MapCenter {
  lat: number;
  lon: number;
  zoom: number;
}

export const useVesselStore = create<VesselStore>((set) => ({...}));
```

From src/lib/db/schema.sql (Phase 2 pattern):
```sql
CREATE TABLE IF NOT EXISTS vessel_sanctions (
  imo VARCHAR(10) PRIMARY KEY,
  sanctioning_authority VARCHAR(10) NOT NULL,
  ...
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and extend database schema</name>
  <files>package.json, src/lib/db/schema.sql</files>
  <action>
1. Install uuid dependency for session-based user identification:
   ```bash
   npm install uuid
   npm install -D @types/uuid
   ```

2. Extend src/lib/db/schema.sql with Phase 3 tables following existing patterns:

   **vessel_anomalies table (ANOM-01, ANOM-02):**
   - id SERIAL PRIMARY KEY
   - imo VARCHAR(10) NOT NULL REFERENCES vessels(imo)
   - anomaly_type VARCHAR(50) NOT NULL (going_dark, loitering, deviation, speed)
   - confidence VARCHAR(20) DEFAULT 'confirmed' (confirmed, suspected, unknown)
   - detected_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   - resolved_at TIMESTAMPTZ (NULL = still active)
   - details JSONB (type-specific data)
   - created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   - Index on (imo, anomaly_type) WHERE resolved_at IS NULL for active anomalies
   - Index on (anomaly_type, detected_at DESC) for type queries

   **watchlist table (HIST-02):**
   - user_id VARCHAR(50) NOT NULL (UUID from localStorage)
   - imo VARCHAR(10) NOT NULL REFERENCES vessels(imo)
   - added_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   - notes TEXT
   - PRIMARY KEY (user_id, imo)
   - Index on (user_id, added_at DESC)

   **alerts table (HIST-02):**
   - id SERIAL PRIMARY KEY
   - user_id VARCHAR(50) NOT NULL
   - imo VARCHAR(10) NOT NULL REFERENCES vessels(imo)
   - alert_type VARCHAR(50) NOT NULL
   - triggered_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   - read_at TIMESTAMPTZ (NULL = unread)
   - details JSONB
   - Index on (user_id, triggered_at DESC) WHERE read_at IS NULL

Add section header comment "-- Phase 3: Anomaly Detection" like Phase 2 pattern.
  </action>
  <verify>
    <automated>grep -c "vessel_anomalies\|watchlist\|alerts" src/lib/db/schema.sql | grep -q "^[3-9]"</automated>
  </verify>
  <done>Schema file contains vessel_anomalies, watchlist, and alerts tables with correct columns and indexes. uuid package in dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create type definitions and geo utilities</name>
  <files>src/types/anomaly.ts, src/lib/geo/haversine.ts, src/lib/geo/haversine.test.ts, src/lib/detection/coverage-zones.ts, src/lib/detection/coverage-zones.test.ts, src/lib/geo/anchorages.ts, src/lib/geo/anchorages.test.ts</files>
  <behavior>
    - haversineDistance(26.0, 56.0, 26.1, 56.0) returns ~11.1 km (0.1 degree latitude)
    - haversineDistance(0, 0, 0, 1) returns ~111 km (1 degree longitude at equator)
    - calculateBearing(0, 0, 1, 0) returns ~0 degrees (due north)
    - calculateBearing(0, 0, 0, 1) returns ~90 degrees (due east)
    - isInCoverageZone(26.5, 56.0) returns true (Persian Gulf)
    - isInCoverageZone(0, 0) returns false (open ocean)
    - getCoverageZone(26.5, 56.0) returns persian_gulf zone object
    - isInAnchorage(25.2, 56.4) returns true (Fujairah)
    - isInAnchorage(26.0, 56.0) returns false (not an anchorage)
    - getAnchorage(29.2, 50.3) returns Kharg Island anchorage
  </behavior>
  <action>
1. Create src/types/anomaly.ts with discriminated union types (from RESEARCH.md):
   - AnomalyType = 'going_dark' | 'loitering' | 'deviation' | 'speed'
   - Confidence = 'confirmed' | 'suspected' | 'unknown'
   - Anomaly interface with type-specific details union
   - GoingDarkDetails, LoiteringDetails, DeviationDetails, SpeedDetails
   - WatchlistEntry interface
   - Alert interface

2. Create src/lib/geo/haversine.ts implementing:
   - haversineDistance(lat1, lon1, lat2, lon2): number (returns km)
   - calculateBearing(lat1, lon1, lat2, lon2): number (returns degrees 0-360)
   Use EARTH_RADIUS_KM = 6371, toRadians helper.

3. Create src/lib/geo/haversine.test.ts with tests for:
   - Known distances (0.1 deg lat at equator = ~11.1 km)
   - Bearing north (0 deg), east (90 deg), south (180 deg), west (270 deg)
   - Edge cases: same point = 0 km, antipodal points

4. Create src/lib/detection/coverage-zones.ts (from RESEARCH.md):
   - CoverageZone interface with id, name, bounds (ChokepointBounds pattern)
   - COVERAGE_ZONES array with 5 zones: persian_gulf, red_sea_north, red_sea_south, suez_approaches, oman_coast
   - isInCoverageZone(lat, lon): boolean
   - getCoverageZone(lat, lon): CoverageZone | null

5. Create src/lib/detection/coverage-zones.test.ts with tests for:
   - Point clearly inside persian_gulf zone
   - Point clearly inside red_sea_south zone
   - Point in open ocean (not in any zone)
   - Point on zone boundary (inclusive check)
   - getCoverageZone returns correct zone object

6. Create src/lib/geo/anchorages.ts (from RESEARCH.md):
   - Anchorage interface with id, name, bounds
   - ANCHORAGES array with 8 anchorages: Fujairah, Kharg Island, Ras Tanura, Jebel Ali, Mina Al Ahmadi, Yanbu, Jeddah, Suez Waiting
   - isInAnchorage(lat, lon): boolean
   - getAnchorage(lat, lon): Anchorage | null

7. Create src/lib/geo/anchorages.test.ts with tests for:
   - Point inside Fujairah anchorage
   - Point inside Kharg Island anchorage
   - Point not in any anchorage
   - getAnchorage returns correct object
  </action>
  <verify>
    <automated>npm run test -- --run src/lib/geo/haversine.test.ts src/lib/detection/coverage-zones.test.ts src/lib/geo/anchorages.test.ts</automated>
  </verify>
  <done>All geo utility tests pass. Types defined. Coverage zones and anchorages defined with bounds matching RESEARCH.md coordinates.</done>
</task>

<task type="auto">
  <name>Task 3: Create CRUD functions and test scaffolds</name>
  <files>src/lib/db/anomalies.ts, src/lib/db/anomalies.test.ts, src/lib/db/watchlist.ts, src/lib/db/watchlist.test.ts, src/lib/db/alerts.ts, src/lib/db/alerts.test.ts</files>
  <action>
1. Create src/lib/db/anomalies.ts with stub implementations:
   - upsertAnomaly(anomaly: UpsertAnomalyInput): Promise<void>
   - getActiveAnomalies(imo?: string): Promise<Anomaly[]>
   - resolveAnomaly(imo: string, anomalyType: string): Promise<void>
   - getAnomaliesForVessels(imos: string[]): Promise<Anomaly[]>
   Use pool from '../db' like existing patterns.

2. Create src/lib/db/anomalies.test.ts with it.todo() scaffolds:
   - describe('upsertAnomaly') with todos for insert, update existing, JSONB details
   - describe('getActiveAnomalies') with todos for all active, filtered by imo
   - describe('resolveAnomaly') with todos for sets resolved_at, idempotent
   - describe('getAnomaliesForVessels') with todos for multiple imos, empty result

3. Create src/lib/db/watchlist.ts with stub implementations:
   - addToWatchlist(userId: string, imo: string, notes?: string): Promise<void>
   - removeFromWatchlist(userId: string, imo: string): Promise<void>
   - getUserWatchlist(userId: string): Promise<WatchlistEntry[]>
   - isOnWatchlist(userId: string, imo: string): Promise<boolean>

4. Create src/lib/db/watchlist.test.ts with it.todo() scaffolds:
   - describe('addToWatchlist') with todos for add, update notes on conflict
   - describe('removeFromWatchlist') with todos for remove, idempotent
   - describe('getUserWatchlist') with todos for returns entries, ordered by added_at
   - describe('isOnWatchlist') with todos for true when present, false when not

5. Create src/lib/db/alerts.ts with stub implementations:
   - createAlert(userId: string, imo: string, alertType: string, details: object): Promise<void>
   - getUnreadAlerts(userId: string): Promise<Alert[]>
   - markAlertAsRead(alertId: number): Promise<void>
   - generateAlertsForAnomaly(imo: string, anomalyType: string, details: object): Promise<void>

6. Create src/lib/db/alerts.test.ts with it.todo() scaffolds:
   - describe('createAlert') with todos for insert, JSONB details
   - describe('getUnreadAlerts') with todos for returns unread only, ordered by triggered_at
   - describe('markAlertAsRead') with todos for sets read_at, idempotent
   - describe('generateAlertsForAnomaly') with todos for creates alerts for watchers, no watchers = no alerts
  </action>
  <verify>
    <automated>npm run test -- --run src/lib/db/anomalies.test.ts src/lib/db/watchlist.test.ts src/lib/db/alerts.test.ts</automated>
  </verify>
  <done>CRUD stub files exist with exported functions. Test files have describe/it.todo scaffolds covering all requirements. Tests run without errors (todos are pending, not failing).</done>
</task>

</tasks>

<verification>
- [ ] uuid in package.json dependencies, @types/uuid in devDependencies
- [ ] Schema SQL has Phase 3 section with vessel_anomalies, watchlist, alerts tables
- [ ] src/types/anomaly.ts exports all required types
- [ ] Haversine tests pass with expected distances and bearings
- [ ] Coverage zone tests pass (inside/outside checks)
- [ ] Anchorage tests pass (inside/outside checks)
- [ ] CRUD files exist with exported functions
- [ ] Test scaffolds have describe blocks with it.todo stubs
- [ ] `npm run test -- --run` passes without failures
</verification>

<success_criteria>
- uuid dependency installed
- Database schema extended with 3 new tables and indexes
- Type definitions provide full type coverage for anomaly system
- Haversine distance accurate to within 1% for test cases
- Coverage zones cover Persian Gulf, Red Sea, Suez approaches
- Anchorages cover major Middle East oil terminals
- CRUD stubs ready for implementation in subsequent plans
- All tests pass (implemented tests green, scaffolds pending)
</success_criteria>

<output>
After completion, create `.planning/phases/03-anomaly-detection/03-01-SUMMARY.md`
</output>
