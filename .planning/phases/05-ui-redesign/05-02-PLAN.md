---
phase: 05-ui-redesign
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/app/(protected)/dashboard/page.tsx
  - src/components/panels/VesselPanel.tsx
  - src/components/panels/OilPricePanel.tsx
  - src/components/panels/NewsPanel.tsx
  - src/components/panels/WatchlistPanel.tsx
autonomous: false
requirements:
  - UI-03
  - UI-04
must_haves:
  truths:
    - "Map and panels sit side-by-side in a grid — no panel uses position: absolute over the map"
    - "Right column panels stack vertically with 1px amber dividers between them"
    - "Data values in panels render with font-mono class at tight spacing"
    - "No rounded corners exist on any panel component"
    - "Dashboard background is bg-black throughout"
  artifacts:
    - path: "src/app/(protected)/dashboard/page.tsx"
      provides: "CSS Grid layout (grid-cols-[1fr_320px]) with map left, panels right"
      contains: "grid grid-cols-"
    - path: "src/components/panels/VesselPanel.tsx"
      provides: "Panel in normal grid flow, no absolute positioning, terminal structure"
    - path: "src/components/panels/OilPricePanel.tsx"
      provides: "Panel in normal grid flow, terminal structure"
    - path: "src/components/panels/NewsPanel.tsx"
      provides: "Panel in normal grid flow, terminal structure"
    - path: "src/components/panels/WatchlistPanel.tsx"
      provides: "Panel in normal grid flow, terminal structure"
  key_links:
    - from: "src/app/(protected)/dashboard/page.tsx"
      to: "src/components/panels/VesselPanel.tsx"
      via: "right column div renders VesselPanel as grid flow child (not absolute)"
      pattern: "flex flex-col.*VesselPanel"
---

<objective>
Restructure the dashboard from floating absolute overlays to a CSS Grid layout, and apply terminal panel styling to all four panels.

Purpose: This is the core structural change of Phase 5. Moving panels out of the z-stack eliminates the floating overlay pattern and satisfies UI-03 (hard grid layout). Terminal panel styling (no rounded corners, tight density) satisfies UI-04.
Output: dashboard/page.tsx with two-column grid; VesselPanel, OilPricePanel, NewsPanel, WatchlistPanel all restyled as terminal grid children.
</objective>

<execution_context>
@/Users/randyren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/randyren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-ui-redesign/05-RESEARCH.md
@.planning/phases/05-ui-redesign/05-01-SUMMARY.md

<interfaces>
<!-- Current dashboard/page.tsx structure (to be replaced) -->
<!-- dashboard/page.tsx currently: -->
<!--   <div className="h-screen flex flex-col bg-[#1a1a2e]"> -->
<!--     <Header ... /> -->
<!--     <main className="flex-1 relative"> -->
<!--       <VesselMap /> -->
<!--       <VesselPanel />       ← absolute positioned -->
<!--       <WatchlistPanel />    ← absolute positioned -->
<!--       <OilPricePanel />     ← absolute positioned -->
<!--       <NewsPanel />         ← absolute positioned -->
<!--     </main> -->
<!--   </div> -->

<!-- All four panels are currently absolute-positioned overlays on top of the map.
     After this plan, they become stacked children in the right grid column. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure dashboard/page.tsx to CSS Grid</name>
  <files>src/app/(protected)/dashboard/page.tsx</files>
  <action>
Replace the `<main>` area from `relative` with absolute children to a two-column CSS Grid where the map occupies the left column and a scrollable panel column occupies the right.

Replace the return JSX in DashboardPage with:

```tsx
return (
  <div className="h-screen flex flex-col bg-black">
    <Header
      onSearchSelect={handleSearchSelect}
      onChokepointSelect={handleChokepointSelect}
    />
    <main className="flex-1 grid grid-cols-[1fr_320px] overflow-hidden max-md:flex max-md:flex-col">
      {/* Left column: full-height map */}
      <div className="relative overflow-hidden">
        <VesselMap />
      </div>
      {/* Right column: stacked panels */}
      <div className="flex flex-col overflow-y-auto bg-black border-l border-amber-500/20 divide-y divide-amber-500/10">
        <VesselPanel />
        <WatchlistPanel />
        <OilPricePanel />
        <NewsPanel />
      </div>
    </main>
  </div>
);
```

Key changes:
- `bg-[#1a1a2e]` → `bg-black`
- `<main className="flex-1 relative">` → `<main className="flex-1 grid grid-cols-[1fr_320px] overflow-hidden max-md:flex max-md:flex-col">`
- VesselMap moves into its own `<div className="relative overflow-hidden">` left grid cell
- Panels move into a right column `<div className="flex flex-col overflow-y-auto bg-black border-l border-amber-500/20 divide-y divide-amber-500/10">`
- Panel render order in right column: VesselPanel, WatchlistPanel, OilPricePanel, NewsPanel

The `max-md:flex max-md:flex-col` handles mobile: on narrow screens, the grid becomes a flex-col so map is full-width above and panels scroll beneath. This replaces the old mobile bottom-sheet behavior.

Do NOT remove any of the callback props or Zustand state logic — only the JSX structure changes.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>dashboard/page.tsx contains `grid grid-cols-[1fr_320px]` and panels are rendered inside the right-column div as flow children. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle all four panels to terminal aesthetic</name>
  <files>
    src/components/panels/VesselPanel.tsx
    src/components/panels/OilPricePanel.tsx
    src/components/panels/NewsPanel.tsx
    src/components/panels/WatchlistPanel.tsx
  </files>
  <action>
For each panel, apply terminal panel styling: remove all absolute positioning, replace purple/navy backgrounds with bg-black, remove all rounded-* classes, and apply the terminal panel header pattern.

**Terminal panel conventions (apply uniformly to all four):**
- Panel header: `px-3 py-1.5 border-b border-amber-500/20 flex items-center justify-between`
- Header label text: `text-xs text-amber-500 font-mono uppercase tracking-widest`
- Content wrapper: `px-3 py-2 space-y-1.5 text-xs`
- Label spans: `text-xs text-gray-500`
- Data value spans: `font-mono text-white text-xs`
- No `rounded-*` anywhere (globals.css @theme wipes them, but remove class strings too for clarity)
- No `p-4` or larger padding — max is `px-3 py-2` or `px-3 py-1.5`
- No absolute, z-10, z-20, z-40 positioning

**VesselPanel.tsx:**
- Remove: `absolute right-0 top-0 bottom-0 w-80`, all z-index classes
- Remove: `max-md:*` absolute-positioning overrides (the mobile bottom-sheet pattern). Keep `max-md:w-full` only if it doesn't reintroduce absolute.
- Remove: `bg-[#16162a]` → replace with `bg-black`
- Remove: `rounded-t-2xl`, `rounded-lg`, `rounded` on all elements
- Add: terminal panel header with "VESSEL DETAIL" label
- Data rows: each field (IMO, Speed, Heading, Destination, Flag, Type) formatted as `<div className="flex justify-between"><span className="text-gray-500">IMO</span><span className="font-mono text-white">9876543</span></div>`
- The component returns null when no vessel selected — preserve that behavior

**OilPricePanel.tsx:**
- Remove: `absolute top-16 right-4` positioning
- Remove: `bg-[#16162a]`, `rounded-lg`
- Add: terminal panel header with "OIL PRICES" label
- Price values (WTI, Brent numbers): ensure `font-mono` class
- Keep Sparkline component, wrap it appropriately

**NewsPanel.tsx:**
- Remove: `absolute top-32 right-4 w-80` positioning
- Remove: `bg-[#16162a]`, `rounded-lg`
- Add: terminal panel header with "INTEL FEED" label
- Headline items: tight `space-y-1.5`, `text-xs leading-tight`

**WatchlistPanel.tsx:**
- Remove: `absolute left-4 top-20` positioning, all z-index classes
- Remove: `bg-[#1e1e3f]/95`, `rounded-lg`, `rounded-t-lg`
- Add: terminal panel header with "WATCHLIST" label
- The null return when watchlist is empty — preserve it

After editing all four panels, run: `npx vitest run` to confirm no test regressions from component changes.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>All four panels: no absolute positioning classes, no rounded-* classes, no bg-[#16162a] or bg-[#1e1e3f] values, terminal panel header present with amber label text. All existing tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  <name>Checkpoint: Verify grid layout and terminal panel styling</name>
    CSS Grid dashboard layout with terminal-styled panels. Map occupies left ~75% of the screen; panels stack in a right 320px column separated by 1px amber-tinted dividers. No panel floats over the map. Panel headers have amber monospace labels. Data rows are tight and compact.
  </what-built>
  <how-to-verify>
  <action>Human verification step — no automated action. Review the built UI against criteria below.</action>
    1. Run `npm run dev` and open http://localhost:3000/dashboard
    2. Confirm: Map is visible on the left, panels stack on the right in a fixed column
    3. Confirm: No panel appears as a floating overlay on top of the map
    4. Open browser DevTools. Inspect any panel element. Confirm `position` is not `absolute` (it should be `static` or `relative` only)
    5. Confirm: Panel backgrounds are true black (#000000), not navy (#1a1a2e) or purple (#16162a)
    6. Confirm: No element (except notification bell count badge) has a border-radius — check computed styles
    7. Resize browser to mobile width (< 768px): map should fill full width, panels should scroll below it
    8. Open http://localhost:3000/analytics and confirm it still loads (colors may not be updated yet — that's Plan 03)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe layout issues found</resume-signal>
</task>

</tasks>

<verification>
- `grep -r "absolute" src/components/panels/` — should return NO positioning-related absolute classes (absolute used for z-stacking is gone)
- `grep -r "bg-\[#1" src/components/panels/` — returns nothing (all hardcoded navy/purple hex gone)
- `grep -r "rounded" src/components/panels/` — returns nothing (rounded classes removed)
- `grep "grid grid-cols" src/app/\(protected\)/dashboard/page.tsx` — returns the grid layout line
- `npx vitest run` — exits 0
</verification>

<success_criteria>
- dashboard/page.tsx uses CSS Grid two-column layout (1fr + 320px)
- All four panels are static-flow children of the right grid column, not absolute overlays
- All panels: bg-black, no rounded-*, terminal header with amber label
- Data values have font-mono class
- Mobile: grid collapses to flex-col (map above, panels below)
- All existing Vitest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-redesign/05-02-SUMMARY.md`
</output>
