---
phase: 05-ui-redesign
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/ui/Header.tsx
  - src/components/ui/SearchInput.tsx
  - src/components/ui/NotificationBell.tsx
  - src/components/ui/TankerFilter.tsx
  - src/components/ui/AnomalyFilter.tsx
  - src/components/ui/TimeRangeSelector.tsx
  - src/components/ui/ChokepointSelector.tsx
  - src/components/ui/ChokepointWidget.tsx
  - src/components/ui/AnomalyBadge.tsx
  - src/components/charts/TrafficChart.tsx
  - src/app/(protected)/analytics/page.tsx
autonomous: false
requirements:
  - UI-01
  - UI-04
  - UI-05
must_haves:
  truths:
    - "Header background is true black with an amber-tinted bottom border"
    - "Active nav link (Live Map or Analytics) shows amber accent, not blue"
    - "No blue color appears anywhere in the UI (no bg-blue-*, text-blue-*, border-blue-*, #3b82f6)"
    - "No hardcoded purple/navy hex values remain in any .tsx file"
    - "Analytics page background is black with no rounded containers"
    - "TrafficChart vessel count line is gray, not blue"
  artifacts:
    - path: "src/components/ui/Header.tsx"
      provides: "Black header with amber active nav state, amber-tinted borders"
      contains: "border-amber-500"
    - path: "src/components/charts/TrafficChart.tsx"
      provides: "COLORS.vesselCount changed from blue to gray-500"
      contains: "6b7280"
    - path: "src/app/(protected)/analytics/page.tsx"
      provides: "Analytics page with bg-black and no rounded-lg containers"
  key_links:
    - from: "src/components/ui/Header.tsx"
      to: "active nav link"
      via: "isAnalytics conditional — active state uses border-amber-500 text-amber-500 bg-amber-500/10"
      pattern: "border-amber-500 text-amber-500"
---

<objective>
Complete the color and border-radius sweep across all remaining UI components — Header, analytics page, chart colors, filter widgets, search, and notification bell.

Purpose: Plan 02 handles the panels and dashboard grid. This plan handles every other component that still carries navy/purple backgrounds, blue accents, or rounded corners. After this plan, no blue or purple exists anywhere in the app.
Output: Header with terminal amber nav, analytics page styled black, TrafficChart with corrected colors, all filter/search/widget components with terminal aesthetic.
</objective>

<execution_context>
@/Users/randyren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/randyren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-ui-redesign/05-RESEARCH.md
@.planning/phases/05-ui-redesign/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Terminal-style Header and all UI widget components</name>
  <files>
    src/components/ui/Header.tsx
    src/components/ui/SearchInput.tsx
    src/components/ui/NotificationBell.tsx
    src/components/ui/TankerFilter.tsx
    src/components/ui/AnomalyFilter.tsx
    src/components/ui/TimeRangeSelector.tsx
    src/components/ui/ChokepointSelector.tsx
    src/components/ui/ChokepointWidget.tsx
    src/components/ui/AnomalyBadge.tsx
  </files>
  <action>
Apply terminal styling to each UI component. Work file-by-file.

**Header.tsx:**
- Container: `bg-[#16162a]` → `bg-black border-b border-amber-500/20`
- Chokepoint sub-row: `border-t border-gray-800/50` → `border-t border-amber-500/10`
- Nav links (both Live Map and Analytics):
  - Active state: replace `bg-blue-600 text-white` with `border-amber-500 text-amber-500 bg-amber-500/10 border`
  - Inactive state: replace `text-gray-400 hover:text-white hover:bg-gray-700 rounded` with `border border-transparent text-gray-500 hover:text-gray-300 hover:border-gray-700`
  - Class structure: `px-3 py-1 text-xs font-mono uppercase tracking-wider border transition-colors`
- Title "Tanker Tracker": keep `text-white font-bold`, change `text-lg` to `text-sm font-mono uppercase tracking-widest text-amber-500`

**SearchInput.tsx:**
- Remove `rounded-lg` from the input wrapper and dropdown container
- `bg-[#16162a]` → `bg-black`
- Dropdown border: `border-gray-700` → `border-amber-500/20`
- Input: add `font-mono text-xs`
- Hover state in dropdown: `hover:bg-gray-700` → `hover:bg-gray-900`

**NotificationBell.tsx:**
- Dropdown container: `bg-[#1e1e3f]` → `bg-black`
- Dropdown border: keep a border but `border-gray-700` → `border-amber-500/20`
- Remove `rounded-lg` from dropdown (keep `rounded-full` on the count badge — intentional exception for circle shape)
- Alert items: replace purple/navy hover states with `hover:bg-gray-900`
- Hardcoded hex `#1e1e3f` and `#252550` → `bg-black` and `hover:bg-gray-900` respectively

**TankerFilter.tsx:**
- `rounded` → remove
- Any `bg-[#...]` → `bg-black`
- Toggle active state: if using blue → change to `border-amber-500 text-amber-500`

**AnomalyFilter.tsx:**
- Same as TankerFilter: remove `rounded`, replace blue with amber for active state

**TimeRangeSelector.tsx:**
- `bg-[#1a1a2e]` → `bg-black`
- Active button: `bg-blue-600` → `bg-amber-500 text-black` (black text on amber is more readable than white)
- Remove `rounded-lg`, `rounded`

**ChokepointSelector.tsx:**
- `bg-[#1a1a2e]` → `bg-black`
- Active button: `bg-amber-600` → `bg-amber-500 text-black` (standardize to amber-500)
- Remove `rounded`

**ChokepointWidget.tsx (in Header):**
- `bg-[#16162a]` or similar → `bg-black`
- Remove `rounded-lg`
- Border: use `border-amber-500/20`

**AnomalyBadge.tsx:**
- Speed variant: `bg-blue-500` / `bg-blue-400` → `bg-amber-600` / `bg-amber-500`
- Remove `rounded` (the badge shape — becomes square per terminal aesthetic)
- All other variants: verify no blue remains

After editing all components, run: `npx vitest run` to confirm no regressions.
  </action>
  <verify>
    <automated>grep -r "bg-blue\|text-blue\|border-blue\|#3b82f6\|#16162a\|#1e1e3f\|#252550\|#1a1a40" /Users/randyren/Developer/tanker-tracker/src/components/ui/ /Users/randyren/Developer/tanker-tracker/src/components/charts/ 2>/dev/null | grep -v "node_modules" | wc -l</automated>
  </verify>
  <done>Grep for all blue/navy/purple values in src/components/ui/ returns 0 matches. All tests pass. Header active nav shows amber accent, not blue.</done>
</task>

<task type="auto">
  <name>Task 2: Fix analytics page styling and TrafficChart colors</name>
  <files>
    src/app/(protected)/analytics/page.tsx
    src/components/charts/TrafficChart.tsx
  </files>
  <action>
**analytics/page.tsx:**
- Outer div: `min-h-screen bg-[#1a1a2e] text-white` → `min-h-screen bg-black text-white`
- Controls bar: `p-4 bg-[#16162a] rounded-lg` → `p-3 bg-gray-900 border border-amber-500/20` (slight visual separation from pure black)
- Labels "Time Range" / "Chokepoints": `text-sm text-gray-400` → `text-xs text-gray-500 font-mono uppercase tracking-wider`
- Error state: `bg-red-900/20 border border-red-500 rounded-lg text-red-400` → remove `rounded-lg`
- Empty state: `bg-[#16162a] rounded-lg` → `bg-gray-900 border border-amber-500/10`
- Page title: `text-2xl font-bold` → `text-sm font-mono uppercase tracking-widest text-amber-500`
- Page subtitle: `text-gray-400 mt-1` → `text-xs text-gray-600 mt-0.5`

**TrafficChart.tsx:**
- Find the COLORS constant (or inline color values). Current: `vesselCount: '#3b82f6'` (blue).
- Change `vesselCount` color: `'#3b82f6'` → `'#6b7280'` (gray-500 — neutral, not blue)
- Keep: `tankerCount: '#f59e0b'` (amber — already correct)
- Keep: `oilPrice: '#22c55e'` (green — acceptable, provides visual distinction from amber and gray)
- Tooltip contentStyle: find `borderRadius: '8px'` or similar → change to `borderRadius: '0'`
- Chart container: any `bg-[#16162a]` → `bg-black`
- Remove `rounded-lg` from chart wrapper

After edits, run: `npx vitest run` to confirm no regressions.
  </action>
  <verify>
    <automated>grep -r "bg-\[#1\|rounded-lg\|bg-blue\|#3b82f6\|#16162a\|#1a1a2e" /Users/randyren/Developer/tanker-tracker/src/app/\(protected\)/analytics/page.tsx /Users/randyren/Developer/tanker-tracker/src/components/charts/TrafficChart.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <done>Analytics page and TrafficChart contain no hardcoded navy/purple hex, no bg-blue, no rounded-lg. TrafficChart COLORS.vesselCount is gray (#6b7280). All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  <name>Checkpoint: Verify terminal color sweep — no blue or purple remaining</name>
    Full color and border-radius sweep. Header is black with amber nav accents. Analytics page is black with terminal-styled controls. TrafficChart vessel count line is gray. No blue, navy, or purple exists anywhere in the app.
  </what-built>
  <how-to-verify>
  <action>Human verification step — no automated action. Review the built UI against criteria below.</action>
    1. Run `npm run dev` and open http://localhost:3000/dashboard
    2. Confirm: Header background is true black. "Tanker Tracker" title is amber.
    3. Confirm: Active nav link (Live Map) has amber border/text/background. No blue highlight visible.
    4. Click a vessel on the map. Confirm: Vessel panel shows data in monospace font at tight spacing.
    5. Check: Notification bell dropdown is black with amber-tinted border. Alert items have no rounded corners.
    6. Check: Search input dropdown is black with no rounded corners.
    7. Open http://localhost:3000/analytics
    8. Confirm: Analytics page background is black. Controls bar has terminal styling.
    9. Confirm: TrafficChart vessel count line is gray (not blue).
    10. Confirm: Active state on Time Range and Chokepoint selectors is amber, not blue.
    11. Open DevTools > Elements. Search for "blue" in any element's class attribute — should find none except intentional uses (if any).
    12. Search for "#1a1a2e", "#16162a", "#1e1e3f" in computed styles — should find none.
  </how-to-verify>
  <resume-signal>Type "approved" if dashboard looks like a Bloomberg terminal. Describe any remaining blue/navy/purple found.</resume-signal>
</task>

</tasks>

<verification>
Full terminal aesthetic check:
- `grep -r "bg-blue\|text-blue\|border-blue" src/` — returns 0 matches
- `grep -r "#1a1a2e\|#16162a\|#1e1e3f\|#252550\|#1a1a40\|#3b82f6" src/` — returns 0 matches
- `grep -r "rounded-lg\|rounded-2xl\|rounded-t-2xl" src/components/ src/app/` — returns 0 matches (only rounded-full on notification badge is acceptable)
- `npx vitest run` — exits 0
</verification>

<success_criteria>
- Header: black background, amber border-bottom, amber active nav (not blue)
- All blue colors (bg-blue-*, text-blue-*, #3b82f6) eliminated from codebase
- All hardcoded purple/navy hex (#1a1a2e, #16162a, #1e1e3f, etc.) eliminated
- Analytics page: black background, terminal-styled controls, no rounded containers
- TrafficChart: vessel count line color changed from blue (#3b82f6) to gray (#6b7280)
- All Vitest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-redesign/05-03-SUMMARY.md`
</output>
