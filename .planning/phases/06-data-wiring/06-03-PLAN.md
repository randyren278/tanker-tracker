---
phase: 06-data-wiring
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified: []
autonomous: false
requirements:
  - WIRE-01
  - WIRE-02
  - WIRE-03
  - WIRE-04
  - WIRE-05
  - WIRE-06

must_haves:
  truths:
    - "Running `npm run ingester` logs connected or failed within 10 seconds"
    - "After ingester runs, oil price panel shows real WTI and Brent values"
    - "After ingester runs, news panel shows real geopolitical headlines"
    - "Status bar is visible in the dashboard header with source indicators"
    - "Anomaly detection crons fire on schedule (visible in ingester console logs)"
  artifacts:
    - path: "src/services/ais-ingester/refresh-jobs.ts"
      provides: "Background refresh for prices/news/sanctions"
    - path: "src/app/api/status/route.ts"
      provides: "System status endpoint"
    - path: "src/components/ui/StatusBar.tsx"
      provides: "Status bar in dashboard header"
  key_links:
    - from: "npm run ingester"
      to: "src/services/ais-ingester/index.ts"
      via: "tsx script in root package.json"
      pattern: "ingester"
---

<objective>
Human verification checkpoint: confirm that all Phase 6 wiring works end-to-end — ingester starts, data flows into the DB, panels show real data, and the status bar reflects actual source health.

Purpose: All automation from plans 01-02 is complete. This checkpoint confirms live behavior that only a human can verify: real API responses, real console output, and the visual dashboard state.
Output: Phase 6 complete — all WIRE requirements confirmed live.
</objective>

<execution_context>
@/Users/randyren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/randyren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-data-wiring/06-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify all Phase 6 data wiring end-to-end</name>
  <what-built>
All Phase 6 data wiring:
- Root `npm run ingester` script that starts the standalone AIS ingester
- Background refresh cron jobs for oil prices (every 6h), news (every 30min), and sanctions (daily) with eager startup fetch
- `node-cron` added to ingester's own package.json for standalone deployment
- GET /api/status endpoint returning live/degraded/offline per source from DB freshness
- StatusBar component in the dashboard header showing AIS, PRICES, NEWS indicators
  </what-built>
  <how-to-verify>
**Step 1 — Verify ingester starts (WIRE-01):**
```
npm run ingester
```
Observe console output within 10 seconds. Expected:
- `Starting anomaly detection cron jobs...` (existing)
- `Refresh cron jobs scheduled: prices every 6h, news every 30m, sanctions daily` (new)
- Either `Connected to AIS stream` or connection error with API key details (depends on env)

**Step 2 — Verify eager startup data fetch (WIRE-02, WIRE-03, WIRE-04):**
After ingester has been running for ~30 seconds, check console for:
- `[CRON] Prices refreshed: 2 symbols` (or similar)
- `[CRON] News refreshed: N headlines`
- `[CRON] Sanctions refreshed: N entries`

If any show errors, note the error message.

**Step 3 — Verify dashboard shows real data (WIRE-02, WIRE-03):**
Open http://localhost:3000/dashboard (ensure Next.js dev server is running: `npm run dev`).
- Oil price panel should show WTI and Brent values with dollar amounts (not blank/null)
- News panel should show real geopolitical headlines (not "No headlines available")

**Step 4 — Verify status bar visible (WIRE-05):**
On the dashboard page, look at the top header bar (right side, after the notification bell):
- Should see `AIS`, `PRICES`, `NEWS` labels with colored dots
- Prices and news should show `live` (amber dot) if ingester ran successfully in Step 2
- AIS may show `offline` or `degraded` depending on whether real AIS data is flowing

**Step 5 — Verify anomaly crons scheduled (WIRE-06):**
In the ingester console from Step 1, verify you see:
- `Starting anomaly detection cron jobs...`
- `Detection cron jobs scheduled:` with the 15-min and 30-min schedule lines

This confirms cron jobs are wired and will fire on schedule.
  </how-to-verify>
  <resume-signal>
Type "approved" if all 5 steps confirm expected behavior. Or describe any failures (e.g., "prices panel still blank", "status bar not visible") so Claude can diagnose.
  </resume-signal>
  <action>Human verification — no automated implementation. See how-to-verify steps above.</action>
  <files>N/A — checkpoint only</files>
  <verify><automated>N/A — checkpoint requires human observation</automated></verify>
  <done>User types "approved" after confirming all 5 verification steps pass</done>
</task>

</tasks>

<verification>
Human confirmed all 5 verification steps pass.
</verification>

<success_criteria>
- WIRE-01: `npm run ingester` starts successfully and logs startup status
- WIRE-02: Oil price panel shows real WTI/Brent data after ingester runs
- WIRE-03: News panel shows real geopolitical headlines after ingester runs
- WIRE-04: Sanctions refresh populates vessel_sanctions table on startup (sanctions flags appear on map after AIS data flows)
- WIRE-05: Status bar visible in header with per-source indicators
- WIRE-06: Anomaly detection crons confirmed scheduled in ingester console
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-wiring/06-03-SUMMARY.md`
</output>
