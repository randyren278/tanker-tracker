---
phase: 07-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified: []
autonomous: false
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04

must_haves:
  truths:
    - "A human has confirmed the README local setup steps are complete and accurate"
    - "A human has confirmed all 8 env vars are documented with correct names and sources"
    - "A human has confirmed the .gitignore excludes .env, .next/, tsconfig.tsbuildinfo, and Docker volumes"
    - "A human has confirmed the production deployment section covers Vercel + Railway/Render"
  artifacts:
    - path: "README.md"
      provides: "Human-verified complete documentation"
      contains: "ALPHA_VANTAGE_API_KEY"
    - path: ".gitignore"
      provides: "Human-verified secret exclusion"
      contains: ".env"
  key_links:
    - from: "human reviewer"
      to: "README.md"
      via: "reading all sections and counting env vars"
      pattern: "manual inspection"
---

<objective>
Human verification checkpoint confirming that Phase 7's documentation deliverables are complete and correct — README covers local setup and production deployment, all 8 env vars are documented, and .gitignore properly excludes secrets and build artifacts.

Purpose: Documentation cannot be verified by automated tests — a human must read it and confirm it makes sense.
Output: Sign-off on DOCS-01 through DOCS-04.
</objective>

<execution_context>
@/Users/randyren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/randyren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-documentation/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated pre-checks before human review</name>
  <files></files>
  <action>
Run the following checks to confirm the documentation artifacts are structurally correct before asking the human to review content:

1. Verify README exists and has all 8 env vars:
```bash
grep -c "ALPHA_VANTAGE_API_KEY\|FRED_API_KEY\|NEWSAPI_KEY\|NEXT_PUBLIC_MAPBOX_TOKEN\|DATABASE_URL\|AISSTREAM_API_KEY\|JWT_SECRET\|PASSWORD_HASH" README.md
```
Expected: 8

2. Verify README has schema.sql reference and production section:
```bash
grep -l "schema.sql" README.md && grep -l "Vercel\|Railway\|Render" README.md
```
Expected: README.md (both)

3. Verify .gitignore covers critical exclusions:
```bash
git check-ignore -v .env .env.local .next tsconfig.tsbuildinfo pgdata .planning/debug
```
Expected: Each path matched by a .gitignore rule

4. Verify .env.example has all 8 vars:
```bash
grep -c "^[A-Z_]\+=" .env.example
```
Expected: 8

If any check fails, fix the issue before proceeding to the human verification task.
  </action>
  <verify>
    <automated>grep -c "ALPHA_VANTAGE_API_KEY\|FRED_API_KEY\|NEWSAPI_KEY\|NEXT_PUBLIC_MAPBOX_TOKEN\|DATABASE_URL\|AISSTREAM_API_KEY\|JWT_SECRET\|PASSWORD_HASH" /Users/randyren/Developer/tanker-tracker/README.md</automated>
  </verify>
  <done>All 4 automated checks pass: README has 8 env vars, README references schema.sql and production hosting, .gitignore covers 6 critical exclusion paths, .env.example has 8 variable assignments.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 7 documentation:
- README.md (created from scratch — did not exist) covering local setup and production deployment
- .gitignore (replaced — was 2 lines, now complete)
- .env.example (updated — was 5 vars, now all 8)
  </what-built>
  <how-to-verify>
Please review each of the following:

**1. Check README local setup (DOCS-01)**
Open README.md and confirm:
- Steps 1–6 are present: Clone/Install, Start TimescaleDB, Apply Schema, Configure Env Vars, Start App, Start Ingester
- The Docker command uses `timescale/timescaledb:latest-pg16` (not plain `postgres:16`)
- The schema step uses `psql ... -f src/lib/db/schema.sql`
- The ingester step uses `npm run ingester`

**2. Check env var documentation (DOCS-02)**
In README.md, find the env var table under "Local Setup > Configure Environment Variables" and confirm:
- All 8 variables are present: DATABASE_URL, AISSTREAM_API_KEY, JWT_SECRET, PASSWORD_HASH, NEXT_PUBLIC_MAPBOX_TOKEN, ALPHA_VANTAGE_API_KEY, FRED_API_KEY, NEWSAPI_KEY
- There is a note explaining the NEXT_PUBLIC_ prefix requirement for Mapbox
- JWT_SECRET and PASSWORD_HASH have generation commands

**3. Check production deployment (DOCS-03)**
Find the "Production Deployment" section and confirm:
- Covers Next.js on Vercel (connect repo, set env vars, deploy)
- Covers AIS ingester on Railway or Render (root dir: src/services/ais-ingester/)
- Covers production database options (Timescale Cloud, Railway PostgreSQL)

**4. Check .gitignore (DOCS-04)**
Run in terminal:
```bash
git check-ignore -v .env .env.local .next tsconfig.tsbuildinfo
```
Confirm each path shows a matching rule (not blank output).

Also confirm there is no .next/ directory showing in `git status` as untracked.
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 7 complete when all 4 requirements verified:
- DOCS-01: README local setup steps confirmed complete and accurate
- DOCS-02: All 8 env vars documented in README and .env.example
- DOCS-03: Production deployment section confirmed present and correct
- DOCS-04: .gitignore confirmed to exclude secrets and build artifacts
</verification>

<success_criteria>
Human reviewer approves all 4 DOCS requirements. Phase 7 documentation is ship-ready — a new user can clone, configure, and run the app using only the README.
</success_criteria>

<output>
After completion, create `.planning/phases/07-documentation/07-03-SUMMARY.md`
</output>
