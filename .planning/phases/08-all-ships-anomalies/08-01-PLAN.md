---
phase: 08-all-ships-anomalies
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/detection/going-dark.ts
  - src/lib/detection/loitering.ts
  - src/lib/detection/deviation.ts
autonomous: true
requirements:
  - ANOM-05

must_haves:
  truths:
    - "Going-dark detection runs on all vessels, not only those with ship_type 80-89"
    - "Loitering detection runs on all vessels regardless of ship type"
    - "Speed anomaly detection runs on all vessels regardless of ship type"
  artifacts:
    - path: "src/lib/detection/going-dark.ts"
      provides: "Going-dark detection query without ship_type filter"
      contains: "detectGoingDark"
    - path: "src/lib/detection/loitering.ts"
      provides: "Loitering detection query without ship_type filter"
      contains: "detectLoitering"
    - path: "src/lib/detection/deviation.ts"
      provides: "Speed anomaly detection query without ship_type filter"
      contains: "detectSpeedAnomaly"
  key_links:
    - from: "src/lib/detection/going-dark.ts"
      to: "vessel_anomalies table"
      via: "upsertAnomaly"
      pattern: "upsertAnomaly"
    - from: "src/lib/detection/loitering.ts"
      to: "vessel_anomalies table"
      via: "upsertAnomaly"
      pattern: "upsertAnomaly"
---

<objective>
Remove the tanker-only ship_type constraint from all three anomaly detection queries so that going-dark, loitering, and speed anomaly detection operate on every vessel type in the AIS stream.

Purpose: ANOM-05 — detection should apply to cargo ships, bulk carriers, and all other vessel types, not just tankers (ship_type 80-89).
Output: Three modified detection files with the ship_type WHERE clause removed. No behavioral change for existing tanker anomalies — they continue to be detected.
</objective>

<execution_context>
@/Users/randyren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/randyren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove ship_type tanker filter from going-dark detection</name>
  <files>src/lib/detection/going-dark.ts, src/lib/detection/going-dark.test.ts</files>
  <behavior>
    - detectGoingDark() query must NOT include `AND v.ship_type BETWEEN 80 AND 89`
    - detectGoingDark() must still only query vessels where last_seen &lt; NOW() - INTERVAL '2 hours'
    - shouldFlagAsGoingDark() pure function behavior is unchanged
    - determineConfidence() pure function behavior is unchanged
  </behavior>
  <action>
    In `src/lib/detection/going-dark.ts`, in the `detectGoingDark()` function, remove the line:
      `AND v.ship_type BETWEEN 80 AND 89`
    from the SQL query (line ~92). The query already filters by `v.last_seen &lt; NOW() - INTERVAL '2 hours'`; the ship_type clause is the only thing to remove. Update the comment above the query from "Query vessels with no update in >2 hours (tankers only, ship_type 80-89)" to "Query all vessels with no update in >2 hours".

    In `src/lib/detection/going-dark.test.ts`, update any existing test that asserts the query only runs on tankers to reflect all-vessel behavior. Add or update a test confirming that a mock vessel with ship_type 72 (cargo) would not be excluded.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=going-dark</automated>
  </verify>
  <done>going-dark.ts query has no ship_type filter; tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove ship_type tanker filter from loitering and speed detection</name>
  <files>src/lib/detection/loitering.ts, src/lib/detection/deviation.ts, src/lib/detection/loitering.test.ts, src/lib/detection/deviation.test.ts</files>
  <behavior>
    - detectLoitering() query must NOT include `WHERE v.ship_type BETWEEN 80 AND 89` (line ~95)
    - detectSpeedAnomaly() query must NOT include `AND v.ship_type BETWEEN 80 AND 89` (line ~60)
    - isLoiteringBehavior() pure function is unchanged
    - isSpeedAnomaly() pure function is unchanged
  </behavior>
  <action>
    In `src/lib/detection/loitering.ts`, in `detectLoitering()`, remove:
      `WHERE v.ship_type BETWEEN 80 AND 89`
    Replace with no ship_type constraint. The HAVING COUNT(*) >= 3 and time filter remain.

    In `src/lib/detection/deviation.ts`, in `detectSpeedAnomaly()`, remove:
      `AND v.ship_type BETWEEN 80 AND 89`
    from the WHERE clause.

    Update comments in both files from "tankers" to "all vessels".

    In the corresponding test files, update or add tests confirming a non-tanker vessel (ship_type 72) is no longer excluded from the queries.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="loitering|deviation"</automated>
  </verify>
  <done>loitering.ts and deviation.ts queries have no ship_type filter; tests pass</done>
</task>

</tasks>

<verification>
Run all detection tests: `npm test -- --testPathPattern="going-dark|loitering|deviation"`
Confirm zero occurrences of `ship_type BETWEEN 80 AND 89` in detection files:
  grep -n "ship_type BETWEEN" src/lib/detection/*.ts  (should return no results)
</verification>

<success_criteria>
- No `ship_type BETWEEN 80 AND 89` constraint exists in any of the three detection files
- All existing tests still pass (no regressions on pure functions)
- TypeScript build succeeds: `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-all-ships-anomalies/08-01-SUMMARY.md`
</output>
